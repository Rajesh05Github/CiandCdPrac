services:
  frontend:
    build:
      context: .
      dockerfile: ./Frontend/dockerfile
    container_name: frontend
    restart: always
    ports:
      - "5173:80"
  
  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    container_name: backend  # Fixed typo here
    restart: always
    ports:  # Changed 'port' to 'ports'
      - "5004:5004"
